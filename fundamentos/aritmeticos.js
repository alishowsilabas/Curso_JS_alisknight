const [a, b, c, d] = [3, 5, 1, 15]
const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2 // o resto da divisão que é armezanado ao modulo, neste caso 3/2 = 2, 1 = resto = modulo

console.log(soma, subtracao, multiplicacao, divisao, modulo)